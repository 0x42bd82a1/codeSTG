这是一个FZU的SE作业
而且只有一个master分支

###使用方法：
1.运行server/serverMain.py
2.选择喜欢的规则来启动游戏
3.运行client/clientMain.py
4.选择端口来观察或手工操作，记得要读操作说明

###使用方法：
agent目录里有一个很随性的样例
名为MyAgent.py，可以参考
没有API，数据格式很简单所以丢给用户自己写。

###贡献者：
策划一个人，放飞自我乱写了一星期。

###其它
策划有病，不用Unity+cs，不套模板贴图+设计关卡，真就当成工程来选题。
让组员直接对着一个物理引擎+一个随便什么能绘图的引擎编写。
听组员说会点python就把Java扔了重写，最后还是什么都交不了。
全员弱鸡。
要求提交一个GUI模块，结果只能拿出不知道哪里抄来的Helloworld，提交前几天才开始慌张觉得好难不会写。
要求提交一个socket模块，结果提交了一个main.py叫全员对着它加内容（反客为主），而且需要的部分还不完整。
连贴图都是策划（随手）画的。
大家一起写文档，然而全体不了解项目，写了一堆套话，还有不少写错的地方。

##Friendship Usually Can Keep.
